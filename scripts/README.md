# Scripts directory

This directory is intended for scripts that serve the whole repo, scripts for
individual project belong in the respective `scripts` directory for that project.

The shared directory is for scripts that are used by multiple projects that
can be symlinked into their `scripts` directories.
