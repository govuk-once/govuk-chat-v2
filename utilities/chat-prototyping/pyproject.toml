[project]
name = "chat-prototyping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
dependencies = [
    "chat-assistants",
    "gradio>=6.0.1",
    "httpx>=0.28.1",
    "httpx-sse>=0.4.3",
]

[dependency-groups]
dev = [
    "pyright>=1.1.406",
    "pytest>=8.4.2",
    "pytest-asyncio>=1.3.0",
    "pytest-mock>=3.15.1",
    "respx>=0.22.0",
    "ruff>=0.14.1",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = [
  ".",
  "src"
]
